import styled from 'styled-components';

const styles = {};

styles.Nav = styled.div`
	  > ul {
	    display: flex;
	    width: 100%;
	    > li {
	      flex-grow: 1;
	    }
	  }
`;

export default styles;
